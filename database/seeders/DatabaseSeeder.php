<?php

namespace Database\Seeders; // <-- O namespace correto é crucial

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Run the database seeders.
     */
    public function run(): void
    {
        // Aqui é onde você chamaria outros seeders (se existirem).
        // Por enquanto, vamos deixá-lo vazio para o comando funcionar.
    }
}